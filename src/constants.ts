export enum CollectionNames {
    MEMBERS = "members",
    CHECK_IN = "checkIn",
}

export enum MemberFields {
    NAME = "name",
    CREATED_AT = "createdAt",
}

export enum CheckInFields {
    DATE = "date",
    MEMBERS = "members",
}

export enum QueryKeys {
    MEMBER = "member",
    MEMBERS = "members",
    CHECK_IN = "checkIn",

    ALL_MEMBERS = "all_members",
    CHECK_IN_ALL = "checkInAll",
}
